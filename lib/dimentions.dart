import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double pageViewContainer = screenHeight / 1.86;
  static double pVCContaintainer = screenHeight / 2.70;
  static double pageViewContainerText = screenHeight / 4.96;
}
